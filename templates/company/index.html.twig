{% extends 'base.html.twig' %}

{% block title %}{{ page_title }}{% endblock %}

{% block body %}
<div class="container p-2">
    <div class="d-flex justify-content-between flex-wrap">
        <h1>{{ page_title }}</h1>
{#        align button bottom#}
        <div class="d-flex align-items-end mb-2">
            <a href="{{ path('app_company_add') }}" class="btn btn-primary">Ajouter</a>
        </div>

    </div>

    <div class="row justify-content-center">
        <div class="col-12">
            {% include 'partials/_paginated-data-table.html.twig' with {'data': paginated_data } %}
        </div>
    </div>
</div>
{% endblock %}
